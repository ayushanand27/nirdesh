# Copy to .env.local for local overrides (optional).
# Leave unset for local dev — the Vite dev server proxies /api to localhost:8000.
# In production (Render Static Site), set this to the deployed backend URL:
#   VITE_API_BASE_URL=https://nirdesh-backend.onrender.com
VITE_API_BASE_URL=
